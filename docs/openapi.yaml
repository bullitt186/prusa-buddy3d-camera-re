# Prusa Connect Camera API — OpenAPI specification v0.22.0
#
# This file is Prusa Research's published Camera API spec and is NOT covered
# by this repository's MIT license. It is included here solely for reference.
#
# Source: https://connect.prusa3d.com/docs/cameras/openapi/
# Archived: 2026-07-09
#
# How this was used in the RE: it pinned the camera origin enum (WEB/OTHER/LINK),
# the /c/info and /c/snapshot schemas, and confirmed that WebRTC/Socket.IO are
# not described here (firmware-only knowledge). See docs/sources.md for details.
#
# To obtain the current version: https://connect.prusa3d.com/docs/cameras/openapi/
