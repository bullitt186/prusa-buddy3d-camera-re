# Image-only drop-in ordering NetworkManager after durable storage is ready
# (source §3.2: data-ready.target -> NetworkManager.service). Ordering only:
# NetworkManager must not hard-Require data-ready.target, because the WP-3
# provisioning hotspot is the recovery path when DATA is missing or corrupt.
[Unit]
After=data-ready.target
